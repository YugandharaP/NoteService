server:
  port: 8600

spring:
  application:
    name: noteService
  cloud: 
    config: 
      discovery: 
        service-id: toDoConfigServer
      uri: http://localhost:8700
      profile: dev
  profiles:
    active:
    - dev